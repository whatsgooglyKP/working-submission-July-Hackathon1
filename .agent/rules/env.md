# Python Environment Rules

Always use the Python interpreter at `.\.venv\Scripts\python.exe` (on Windows) or `./.venv/bin/python` (on Unix/Linux) relative to the workspace root.
When executing commands, install dependencies using `.\.venv\Scripts\pip.exe` and execute Python scripts using `.\.venv\Scripts\python.exe`.
